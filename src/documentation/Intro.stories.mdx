import { Meta } from '@storybook/addon-docs';
import { OVERVIEW } from '@/constants/categories';
import Contributors from './Intro.components.tsx';
import { ThemeProvider } from '@/components/ThemeProvider';
import Text from '@/components/Text';

export const builtText =
  process.env.CI_COMMIT_REF_NAME ||
  (process.env.GIT_IS_TAG_DIRTY ? process.env.GIT_BRANCH : process.env.GIT_TAG);

<Meta
  title={`${OVERVIEW}/Introduction`}
  parameters={{
    fullPage: true,
    previewTabs: {
      canvas: { hidden: true },
    },
    viewport: {
      defaultViewport: 'responsive',
    },
  }}
/>

<ThemeProvider locale='en-GB'>
  <Text color='secondary'>Built from {builtText}</Text>
</ThemeProvider>

# @stf-team/ui-kit

This New UI Kit is supposed to be widely used in [Straigth Forward](https://github.com/straight-forward-team) Team.

Components are based on [Figma Design System](coming soon).

## Table of Contents

- [Installation](?path=/docs/overview-installation--page)
- [Custom Styling](?path=/docs/overview-custom-styling--page)
- [Fonts](?path=/docs/overview-fonts--page)
- [Contribution](?path=/docs/overview-contribution--page)

## Changelog

You can checkout [Github Release Page](coming soon) or ["CHANGELOG.md"](https://github.com/straight-forward-team/stf-ui/blob/develop/CHANGELOG.md) to inform about recent changes and releases of the `@stf-team/ui-kit` package.

## Gratitude

Thanks to everyone who has contributed to the development and improvement of the @stf-team/ui-kit so far.

<Contributors />
